name: Go

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
  pull_request:
    branches: [ main ]
    paths-ignore:
      - '**.md'

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - name: Check out reg-paygate-adapter (primary)
      uses: actions/checkout@v2
      with:
        path: ./reg-paygate-adapter

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: '1.20'

    - name: Build reg-paygate-adapter (primary)
      run: go build -v ./...
      working-directory: ./reg-paygate-adapter

    - name: Test reg-paygate-adapter (primary)
      run: go test -v ./...
      working-directory: ./reg-paygate-adapter
